<%= form_for(@residue) do |f| %>
  <% if @residue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@residue.errors.count, "error") %> prohibited this residue from being saved:</h2>

      <ul>
      <% @residue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name_redidue %><br>
    <%= f.text_field :name_redidue %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.text_field :category_id %>
  </div>
  <div class="field">
    <%= f.label :available_quantity %><br>
    <%= f.text_field :available_quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
