<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :recycling_line %><br>
    <%= f.text_field :recycling_line %>
  </div>
  <div class="field">
    <%= f.label :environmental_impact %><br>
    <%= f.text_field :environmental_impact %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
